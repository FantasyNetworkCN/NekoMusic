# NekoMusic 后端文档

## 项目概述

NekoMusic 是一个前后端分离的音乐平台后端服务，使用 Java 和 Jetty 实现，不依赖 Spring Boot 框架。当前版本专注于音乐搜索功能。

## 技术栈

- Java 21
- Jetty 11 (HTTP 服务器)
- Jakarta Servlet API
- Jackson (JSON 处理)
- Jackson YAML (YAML 处理)
- MySQL (数据库)
- HikariCP (数据库连接池)

## 项目结构

```
backend/
├── src/main/java/com/neko/music/
│   ├── Main.java                 # 主启动类
│   ├── config/                   # 配置管理
│   │   └── ConfigManager.java    # 配置管理器
│   ├── database/                 # 数据库管理
│   │   └── DatabaseManager.java  # 数据库管理器
│   └── handlers/                 # API处理器
│       └── MusicSearchHandler.java # 音乐搜索处理器
├── src/main/resources/
│   └── config.yml                # 配置文件
└── pom.xml                       # Maven配置
```

## 配置文件

### config.yml
```yaml
port: 8080
mysql:
  host: localhost
  port: 3306
  database: nek_music
  username: root
  password: ""
```

## API 接口

### 音乐搜索
- **端点**: `POST /api/music/search`
- **功能**: 搜索音乐
- **请求体**:
```json
{
  "query": "搜索关键词"
}
```
- **响应**:
```json
{
  "success": true,
  "message": "搜索成功",
  "results": [
    {
      "id": 1,
      "title": "音乐标题",
      "artist": "艺术家",
      "album": "专辑",
      "duration": 180,
      "filePath": "文件路径",
      "uploadUserId": 1,
      "createdAt": "2023-01-01T00:00:00"
    }
  ]
}
```

## 数据库设计

### users 表
```sql
CREATE TABLE users (
  id INT AUTO_INCREMENT PRIMARY KEY,
  username VARCHAR(50) UNIQUE NOT NULL,
  password VARCHAR(255) NOT NULL,
  email VARCHAR(100),
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

### music 表
```sql
CREATE TABLE music (
  id INT AUTO_INCREMENT PRIMARY KEY,
  title VARCHAR(255) NOT NULL,
  artist VARCHAR(255) NOT NULL,
  album VARCHAR(255),
  duration INT,
  file_path VARCHAR(500),
  upload_user_id INT,
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  FOREIGN KEY (upload_user_id) REFERENCES users(id)
);
```

## 依赖项

需要以下 JAR 文件放在 `lib/` 目录:

1. jetty-server-11.0.24.jar
2. jetty-servlet-11.0.24.jar
3. jakarta.servlet-api-5.0.0.jar
4. jackson-databind-2.15.2.jar
5. jackson-core-2.15.2.jar
6. jackson-annotations-2.15.2.jar
7. jackson-dataformat-yaml-2.15.2.jar
8. mysql-connector-java-8.0.33.jar
9. HikariCP-5.0.1.jar

## 编译和运行

### 手动编译
1. 创建 `lib` 目录并下载依赖项
2. 运行编译命令:
```
javac -cp "src/main/java;src/main/resources;lib/*" -d target/classes src/main/java/com/neko/music/*.java src/main/java/com/neko/music/config/*.java src/main/java/com/neko/music/database/*.java src/main/java/com/neko/music/handlers/*.java
```

### 启动服务器
```
java -cp "target/classes;src/main/resources;lib/*" com.neko.music.Main
```

服务器将在配置文件指定的端口上启动。